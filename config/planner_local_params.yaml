TEBLocalPlannerROS:
  #meter_scoring: true  
    # Trajectory
    
  teb_autosize: True
  dt_ref: 0.3
  dt_hysteresis: 0.1
  global_plan_overwrite_orientation: True
  max_global_plan_lookahead_dist: 2.5
  feasibility_check_no_poses: 5
  
  min_turning_radius: 0.0
# Robot Configuration Parameters
  # Acceleration parameters
  acc_lim_x: 2.5
  acc_lim_y: 2.5
  acc_lim_theta: 3.2

  max_vel_x: 0.3
  min_vel_x: -0.05
  max_vel_x_backwards: 0.15
  max_vel_y: 0.3
  min_vel_y: -0.3
  max_vel_theta: 1
  min_vel_theta: -1

  min_in_place_vel_theta: 0.3

  holonomic_robot: true
  escape_vel: -0.3

  # Goal Tolerance Parameters
  yaw_goal_tolerance: 0.15
  xy_goal_tolerance: 0.2
  free_goal_vel: False

  # latch_xy_goal_tolerance: false

  # Forward Simulation Parameters
  #sim_time: 2.0
  #sim_granularity: 0.02 #the step size to take between pointson a trajectory
  #angular_sim_granularity: 0.02
  #vx_samples: 3 #determine how many translational velocity samples to take in x direction
  #vy_samples: 10
  #vth_samples: 20 #controls the number of rotational velocities samples, in most cases higher than vx
  
  #controller_frequency: 40.0

  # Trajectory scoring parameters
  #meter_scoring: true # Whether the gdist_scale and pdist_scale parameters should assume that goal_distance and path_distance are expressed in units of meters or cells. Cells are assumed by default (false).
  #occdist_scale: 0.001 #The weighting for how much the controller should attempt to avoid obstacles. default 0.01
  #path_distance_bias: 0.75 #     The weighting for how much the controller should stay close to the path it was given . default 0.6
  #goal_distance_bias: 1.0 #     The weighting for how much the controller should attempt to reach its local goal, also controls speed  default 0.8

  #heading_lookahead: 0.325 #How far to look ahead in meters when scoring different in-place-rotation trajectories
  heading_scoring: false #Whether to score based on the robot's heading to the path or its distance from the path. default false
  #heading_scoring_timestep: 0.8 #How far to look ahead in time in seconds along the simulated trajectory when using heading scoring (double, default: 0.8)
  #dwa: true #Whether to use the Dynamic Window Approach (DWA)_ or whether to use Trajectory Rollout
  #simple_attractor: false
  #publish_cost_grid_pc: true

  # Oscillation Prevention Parameters
  oscillation_recovery: true 
  #oscillation_reset_dist: 0.25 #How far the robot must travel in meters before oscillation flags are reset (double, default: 0.05)
  #escape_reset_dist: 0.1
  #escape_reset_theta: 0.1
  
  # Obstacles
      
  min_obstacle_dist: 0.15
  include_costmap_obstacles: True
  costmap_obstacles_behind_robot_dist: 1.0
  obstacle_poses_affected: 30
  costmap_converter_plugin: ""
  costmap_converter_spin_thread: True
  costmap_converter_rate: 5
  inflation_dist: 0.15

  # Optimization
      
  no_inner_iterations: 5
  no_outer_iterations: 4
  optimization_activate: True
  optimization_verbose: False
  penalty_epsilon: 0.1
  weight_max_vel_x: 2
  weight_max_vel_theta: 1
  weight_acc_lim_x: 1
  weight_acc_lim_theta: 1
  weight_kinematics_nh: 1000
  weight_kinematics_forward_drive: 100
  weight_kinematics_turning_radius: 1
  weight_optimaltime: 1
  weight_obstacle: 50
  weight_dynamic_obstacle: 10 # not in use yet
  selection_alternative_time_cost: False # not in use yet

  # Homotopy Class Planner

  enable_homotopy_class_planning: True
  enable_multithreading: True
  simple_exploration: False
  max_number_classes: 3
  roadmap_graph_no_samples: 15
  roadmap_graph_area_width: 6
  h_signature_prescaler: 1 ##0.5
  h_signature_threshold: 0.1
  obstacle_keypoint_offset: 0.1
  obstacle_heading_threshold: 1 ##0.45
  visualize_hc_graph: False
